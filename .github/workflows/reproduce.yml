name: Reproduce

# Workflow to verify the project's claims can be reproduced from a clean checkout.
# Triggered manually or weekly to ensure external users can replicate the test suites
# and the offline benchmark scorer without needing any project-specific state.

on:
  workflow_dispatch: {}
  schedule:
    # Weekly Sunday 2am UTC
    - cron: '0 2 * * 0'

jobs:
  reproduce:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout (fresh clone, no reuse of cached state)
        uses: actions/checkout@v7

      - name: Setup Node.js LTS
        uses: actions/setup-node@v7
        with:
          node-version: lts/*

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install Python test dependencies
        # pytest is a hard import for several tracked test modules (test_cli_base,
        # test_status_publish, ...). Omitting it made the full-suite discover step
        # below fail on "No module named 'pytest'". Matches ci.yml / main-full.yml.
        run: pip install pyyaml pytest

      - name: Configure git (required for hook installation tests)
        run: |
          git config --global user.email "ci@example.com"
          git config --global user.name "CI Runner"

      - name: Node syntax check
        run: |
          # *.template.mjs are Claude Code WORKFLOW scripts (top-level return is valid
          # in the harness runtime, illegal to plain node) — structurally tested in
          # tests/buildsystem-template.test.mjs instead.
          git ls-files '*.mjs' | grep -v '\.template\.mjs$' | while read file; do
            node --check "$file" || exit 1
          done

      - name: Shell syntax check
        run: |
          git ls-files '*.sh' | while read file; do
            bash -n "$file" || exit 1
          done

      - name: Run Node.js tests (full suite)
        run: npm run test:node

      - name: Run shell test suites
        run: npm run test:sh

      - name: Run React component tests (vitest)
        run: |
          cd ui/web
          npm ci
          npx vitest run

      - name: Tool import/compile smoke gate
        run: |
          python -m compileall -q tools/
          python -m unittest tests.test_tools_importable -v

      - name: Run Python tests (full suite discover)
        run: |
          python3 << 'EOF'
          import subprocess
          import sys
          import unittest
          from pathlib import Path

          # Get tracked test files only (exclude WIP untracked files)
          try:
              result = subprocess.run(
                  ['git', 'ls-files', 'tests/test_*.py'],
                  capture_output=True,
                  text=True,
                  check=True
              )
              tracked_files = result.stdout.strip().split('\n') if result.stdout.strip() else []
              # Extract just the stem for test discovery
              test_files = sorted(set(Path(f).stem for f in tracked_files if f))
          except Exception as e:
              print(f"ERROR: Failed to get tracked test files: {e}")
              sys.exit(1)

          if not test_files:
              print("ERROR: No tracked test files found")
              sys.exit(1)

          print(f"Running {len(test_files)} tracked test modules")
          loader = unittest.TestLoader()
          suite = unittest.TestSuite()
          failed_imports = []
          skipped_modules = []

          for test_name in test_files:
              try:
                  module = __import__(f'tests.{test_name}', fromlist=[test_name])
                  suite.addTests(loader.loadTestsFromModule(module))
              except unittest.SkipTest as e:
                  # A module-level `raise unittest.SkipTest` is a deliberate,
                  # self-documenting skip that both pytest and `unittest discover`
                  # honor. It subclasses Exception, so the generic handler below
                  # recorded it as an import FAILURE and reddened the whole run.
                  # Mirrors the same fix in tools/ci_shard_runner.py.
                  skipped_modules.append((test_name, str(e)))
                  print(f"SKIP: {test_name}: {e}")
              except Exception as e:
                  failed_imports.append((test_name, str(e)))
                  print(f"ERROR: Failed to load {test_name}: {e}")

          if skipped_modules:
              print(f"\n{len(skipped_modules)} test module(s) skipped at import:")
              for name, reason in skipped_modules:
                  print(f"  - {name}: {reason}")

          # If there were import failures, exit immediately
          if failed_imports:
              print(f"\n{len(failed_imports)} test module(s) failed to import:")
              for name, error in failed_imports:
                  print(f"  - {name}: {error}")
              sys.exit(1)

          # If no tests were collected, this is also an error
          if suite.countTestCases() == 0:
              print("ERROR: No tests were collected")
              sys.exit(1)

          runner = unittest.TextTestRunner(verbosity=2)
          result = runner.run(suite)
          sys.exit(0 if result.wasSuccessful() else 1)
          EOF

      - name: Run benchmark scorer offline (test_bench_runner)
        run: python -m unittest tests.test_bench_runner -v

      - name: Verify offline benchmark reproduction
        run: |
          python tools/bench_runner.py --runner mock
          echo "✓ Benchmark reproduction successful"

      - name: Cache Playwright browsers
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/package.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright and Chromium
        run: pip install playwright && playwright install --with-deps chromium

      - name: Run dashboard browser proof (verify_dash)
        run: python tools/verify_dash.py

      - name: Run submit encoding browser proof (verify_submit_encoding)
        run: python tools/verify_submit_encoding.py

      - name: Run prboard browser proof (verify_prboard)
        run: python tools/verify_prboard.py

      - name: Run agent inspector browser proof (verify_agent_inspector)
        run: python tools/verify_agent_inspector.py

      - name: Run activity filter browser proof (verify_activity_filter)
        run: python tools/verify_activity_filter.py

      - name: Run failure drilldown browser proof (verify_failure_drilldown)
        run: python tools/verify_failure_drilldown.py

      - name: Run wave telemetry browser proof (verify_wave_telemetry)
        run: python tools/verify_wave_telemetry.py

      - name: Run dispatch panel browser proof (verify_dispatch_panel)
        run: python tools/verify_dispatch_panel.py

      - name: Run scorecards browser proof (verify_scorecards)
        run: python tools/verify_scorecards.py

      - name: Run UI trio browser proof (verify_ui_trio)
        run: python tools/verify_ui_trio.py
