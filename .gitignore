# State and transient files
state/
# Monitor runtime outputs (regenerated each cycle; PROPOSALS.md stays tracked — it is durable, human-reviewed)
monitor/BRIEF.md
monitor/SIGNALS.json
*.log
.fleet-repos.json
.signal-state.json
.fleet-ledger-harvest.json
.fleet-prompts-seen.json

# Local configuration (users create their own aesop.config.json)
aesop.config.json
.env
.env.local

# Ledger and runtime data
ledger/
*-BACKUP.log
*.jsonl
bench/results/frontier_slice_results.json

# Exceptions: committed benchmark ground truth and sampled task sets
!bench/tasks_sampled_judgment.jsonl
!bench/ground_truth_sampled.jsonl
!bench/tasks_frontier.jsonl
!bench/ground_truth_frontier.jsonl

# System artifacts
.git/
.DS_Store
*.swp
*~
node_modules/
__pycache__/
*.pyc

# Test output artifacts
node_test_output.txt
*_test_output.txt

# Web dashboard (wave-14): node_modules ignored; ui/web/dist IS committed
# (D2: clone-and-run — python ui/serve.py serves the committed build).
ui/web/node_modules/
!ui/web/dist/
!ui/web/dist/**
monitor/.monitor-heartbeat
monitor/.signal-state.json
monitor/.ledger-cursor.json
!bench/transcript_tasks/tasks_transcript.jsonl
!bench/transcript_tasks/ground_truth_transcript.jsonl
