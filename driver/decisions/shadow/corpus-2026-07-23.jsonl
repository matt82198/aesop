{"id": "vbs-waitforexit", "finding_text": "A Windows VBScript launcher used by scheduled tasks calls shell.Run with waitForExit=False and exits immediately. Lens claims P0: the task instance closes instantly so MultipleInstances IgnoreNew cannot prevent overlapping runs, ExecutionTimeLimit can never kill a hung child, and LastTaskResult is always 0 even when the child fails. The two production daemons launched this way have their own script-level atomic lockfiles.", "source_lens": "windows_process_control", "evidence": ["Code: shell.Run(cmd, 0, False) — second parameter 0 = hidden window, third False = do not wait", "Behavior: script calls shell.Run and immediately exits, child process continues in background", "Impact: task engine's LastTaskResult shows 0 (launcher exit) not child's actual exit code"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fix shipped (wait+exit-code propagation); LastTaskResult evidence confirmed"}}
{"id": "dryrun-blocked", "finding_text": "An installer script validates that bash.exe and a helper file exist BEFORE reaching its -DryRun branch, so preview mode exits 1 on machines without the runtime installed.", "source_lens": "installer_validation", "evidence": ["Code: if ( -not ( Test-Path $bash ) ) { exit 1 } occurs at param validation, before DryRun branch", "Behavior: the check runs on every invocation, including preview/dry-run mode", "Impact: -DryRun flag cannot run on machines without bash.exe; preview is blocked with exit 1"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fixed: warnings in DryRun, exit 0 verified by test"}}
{"id": "uninstall-exit0", "finding_text": "The installer's -Uninstall mode catches unregister exceptions, prints them, and continues; the script exits 0 even when a task failed to unregister, so partial uninstall masquerades as success.", "source_lens": "error_handling", "evidence": ["Code: catch { Write-Error $_; } followed by exit 0 at end of script", "Behavior: exceptions in the catch block do not set a failure flag; script reaches final exit 0", "Impact: a caller checking exit status sees success (0) even when a task failed to unregister"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fixed: bool tracking, exit 1 on real failures"}}
{"id": "quote-validation", "finding_text": "Task commands are interpolated into a double-quoted argument string with no validation that they are free of double quotes; a command containing a quote produces a malformed action string at runtime. A second independent lens reported the same line.", "source_lens": "command_injection", "evidence": ["Code: action = \"cmd.exe /c \" + commandVar with no quote escaping", "Behavior: if commandVar contains a quote, the resulting action string is syntactically invalid", "Impact: the scheduled task's action field becomes malformed at runtime"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fixed: early rejection with exit 1"}}
{"id": "apostrophe-path", "finding_text": "A derived POSIX path is wrapped in single quotes without escaping; a repository path containing an apostrophe yields invalid bash syntax in the default command.", "source_lens": "path_escaping", "evidence": ["Code: cmdDefault = \"bash -c 'cd $repoPath; ...'\" with repoPath not escaped", "Behavior: if repoPath contains an apostrophe, the quote context is broken inside the single-quoted string", "Impact: bash receives a malformed command and exits with a parse error"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fixed: detect + error directing user to explicit command"}}
{"id": "unc-paths", "finding_text": "A Windows-to-POSIX path converter silently mangles UNC paths (\\\\server\\share) into invalid bash paths.", "source_lens": "path_conversion", "evidence": ["Code: UNC path converter transforms \\\\server\\share but the semantic mapping is unclear", "Behavior: Git Bash and MSYS2 both accept //server/share as valid UNC path syntax", "Semantic fact: the claim 'invalid bash path' may overstate the issue; UNC works in these shells"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fixed: detection + clear error + doc note | LABEL DISPUTED 2026-07-23 by fable-blind rung: MSYS/Git-Bash accepts //server/share, so the 'invalid bash path' mechanism is unproven; reject-UNC fix remains defensible but classification may overstate; treat this item as disputed in scoring."}}
{"id": "hardcoded-username", "finding_text": "Documentation examples shipped in a public npm package embed a machine-specific absolute path including the developer's local username, in two places.", "source_lens": "docs_scan", "evidence": ["Code: documentation contains absolute path ~/aesop/state/tracker.json", "Behavior: users copying the path get a non-functional absolute path with the developer's username", "Impact: documentation examples are not portable; users cannot follow the instructions on their machines"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "fixed: placeholders"}}
{"id": "audit-log-observability", "finding_text": "Task registrations are only logged to console stdout; an operator auditing a machine a week later has no persistent record of what was registered, when, with what commands.", "source_lens": "observability_audit", "evidence": ["Current: console.log('Registered task...') writes only to stdout, lost after process ends", "Gap: no persistent record file or system event log entry for audit trails", "Enhancement: adds operational visibility and compliance/debugging capability"], "labels": {"incumbent_verdict": "enhancement_opportunity", "ground_truth": "enhancement_opportunity", "gt_note": "filed as refinement, correctly not blocking"}}
{"id": "whitelist-gate-weakening", "finding_text": "Adding directory names 'daemon' and 'jobs' to a health-check whitelist is claimed MEDIUM gate-weakening: future files inside those directories (e.g. daemon/api-key.json) would not be flagged, and it sets precedent for whitelisting directories without file-level review.", "source_lens": "security_gate", "evidence": ["Mechanism: health check scans top-level directory names only, not recursive subdirectories", "Implementation: the gate operates file-by-file at root level; paths like daemon/* are not in scope", "Semantic fact: adding 'daemon' to whitelist allows the directory itself only, not its contents"], "labels": {"incumbent_verdict": "false_positive", "ground_truth": "false_positive", "gt_note": "refuted on evidence: top-level-only check semantics"}}
{"id": "ps1-syntax-gate", "finding_text": "A new PowerShell file class shipped in the package has zero syntax validation in CI, unlike node --check for JS and bash -n for shell.", "source_lens": "ci_gates", "evidence": ["Current: PowerShell files in package are not validated; JS has 'node --check', shell has 'bash -n'", "Tools available: pwsh -NoProfile -Command or 'bash -n' can validate PowerShell syntax", "Enhancement: adds parity to CI gates across all script languages"], "labels": {"incumbent_verdict": "enhancement_opportunity", "ground_truth": "enhancement_opportunity", "gt_note": "real gap, correctly classified process-level not code defect"}}
{"id": "test-hardcoded-path", "finding_text": "A newly added test hardcodes an absolute machine-specific worktree path in setUpClass; on the CI runner that path does not exist, so setUpClass raises and the whole suite ERRORS on a required check.", "source_lens": "test_portability", "evidence": ["Code: wt_path = '~/aesop-wt-test' (absolute, developer-machine-specific path)", "Behavior: CI runner does not have this path; setUpClass raises FileNotFoundError", "Impact: test suite skips entirely on any machine without the hardcoded path"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "confirmed: would have failed required windows CI; fixed to __file__-relative"}}
{"id": "fixreview-parents1", "finding_text": "Re-attack claim: Path(__file__).resolve().parents[1] used to locate the repo root from a test file might misresolve on CI runner paths or paths with spaces.", "source_lens": "adversarial_reattack", "evidence": ["Mechanism: Path(__file__).resolve() normalizes to absolute path; .parents[1] is the grandparent directory", "Semantic: from tests/ subdirectory, parents[0] is tests/, parents[1] is repo root (true on any filesystem)", "Verification: Path.resolve() handles CI paths and spaces correctly; no special cases break the invariant"], "labels": {"incumbent_verdict": "false_positive", "ground_truth": "false_positive", "gt_note": "verified sound: tests/ parent is repo root on any path incl. runner style"}}
{"id": "fixreview-backtick-test", "finding_text": "Tautology-check claim: a regression test asserting the absence of a backtick character in derived output would not actually catch the original path-corruption bug it was written for.", "source_lens": "adversarial_reattack", "evidence": ["Evidence: the original bug produced backticks in output: ~/aesop\\`whoami`", "Test logic: assert '`' not in result would correctly fail if the bug regresses", "Semantic: the test assertion accurately checks for the symptom that distinguishes buggy vs fixed output"], "labels": {"incumbent_verdict": "false_positive", "ground_truth": "false_positive", "gt_note": "mentally executed against buggy output; would fail correctly"}}
{"id": "regression-ui-suite", "finding_text": "In a fresh review worktree, the UI test harness fails because TypeScript is missing from node_modules; reported as a possible regression in the change under review.", "source_lens": "regression_check", "evidence": ["Mechanism: node_modules is git-ignored; fresh worktree never contains built dependencies", "CI environment: CI runner runs 'npm install' before UI tests, so the suite runs there", "Semantic fact: this is not a regression in the code change; it is a fresh-environment dependency gap"], "labels": {"incumbent_verdict": "false_positive", "ground_truth": "false_positive", "gt_note": "environmental: fresh worktree lacks npm install; CI runs its own install and browser-proofs"}}
{"id": "cimergewait-exit0", "finding_text": "A CI-wait-and-merge helper printed 'CI FAILED: <check>' and correctly refused to merge, but exited with status 0; background callers treating exit code as success are misled about the outcome.", "source_lens": "script_exit_codes", "evidence": ["Code: script prints 'CI FAILED: <check>' and returns without calling exit 1", "Behavior: the script exits with status 0 (success) after refusing the merge", "Impact: a caller checking exit code sees success even though CI failed"], "labels": {"incumbent_verdict": "real_defect", "ground_truth": "real_defect", "gt_note": "observed live; later a second fail-open path in the same tool was confirmed P1"}}
{"id": "vbs-syntax-validity", "finding_text": "Re-attack claim: 'rc = shell.Run(cmd, 0, True)' function-call syntax may be invalid VBScript that fails silently under wscript //B, making the exit-code propagation fix a no-op.", "source_lens": "adversarial_reattack", "evidence": ["Reference: VBScript shell.Run(strCommand, [intWindowStyle], [bWaitOnReturn]) accepts this call form", "Semantics: rc = shell.Run(cmd, 0, True) correctly assigns return value to variable", "Verification: LastTaskResult field in scheduled task engine confirms exit code propagation works"], "labels": {"incumbent_verdict": "false_positive", "ground_truth": "false_positive", "gt_note": "LastTaskResult evidence from registered self-test task"}}
